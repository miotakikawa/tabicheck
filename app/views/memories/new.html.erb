<h2 class="mt-3">New book</h2>
    <%= form_with model: @memory, url:memories_path, local:true do |f| %>
    <h4>Title</h4>
    <%= f.text_field :title %>
    <h4></h4>
    <%= f.attachment_field :image_tag %>
    <h4>opionion</h4>
    <%= f.text_area :body %>
    <%= f.submit 'Create Memory', class: "btn btn-success" %>
    <% end %>
