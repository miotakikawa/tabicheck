<main>
  <p id="notice"></p>
  <div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">



  		<strong>投稿編集<strong>

  		 <div id="error_explanation">
        <% if @memory.errors.any? %>
          <h3><%= @memory.errors.count %> error prohibited this obj from being saved:</h3>
          <ul>
            <li>
            <% @memory.errors.full_messages.each do |message| %>
              <%= message %>
            <% end %>
            </li>
          </ul>
        <% end %>
      </div>


  		<%= form_with model: @memory, url:memory_path, local:true do |f| %>
  		<div class="form-group">
        <label>Title</label>
        <%= f.text_field :title %>
      </div>
      <div class="form-group">
        <label></label>
        <%= f.attachment_field :image %>
      </div>
      <div class="form-group">
        <label>Opinion</label>
        <%= f.text_area :body %>
      </div>
      <div class="form-group">
      <%= f.submit 'Update', class: "btn btn-success" %>
      </div>
      <% end %>
      <a><%=link_to "Show", memory_path %></a> | <a><%=link_to "Back", memories_path %></a>
  	</div>
  </div>
  </div>
  </p>
  </main>
